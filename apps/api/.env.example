# =============================================================================
# API — variáveis de ambiente (copie para .env e preencha)
# =============================================================================

# --- Banco (Supabase Postgres) ---
# Session pooler (porta 5432): Project Settings → Database → Connection string → URI → Session
# Ex.: postgresql://postgres.[REF]:[SENHA]@aws-0-[REGION].pooler.supabase.com:5432/postgres
DATABASE_URL=

# --- JWT (obrigatório em produção) ---
JWT_SECRET=
JWT_REFRESH_SECRET=
# JWT_ACCESS_MINUTES=15
# JWT_REFRESH_DAYS=7

# --- CORS (origens permitidas) ---
# URL do frontend em produção (ex.: https://seu-app.vercel.app)
# Qualquer origem *.vercel.app já é permitida automaticamente (preview e produção).
WEB_ORIGIN=
# Origens adicionais separadas por vírgula (ex.: domínio customizado)
# WEB_ORIGINS=

# --- Servidor ---
# PORT=3001

# --- Storage (uploads) ---
# STORAGE_PROVIDER=local
# STORAGE_LOCAL_DIR=uploads
# STORAGE_BASE_URL=http://localhost:3001/uploads

# --- Redis (BullMQ / filas) ---
# REDIS_URL=redis://localhost:6379

# --- Dev / bypass de auth (não usar em produção) ---
# ALLOW_ANONYMOUS=false

# --- Seed (opcional) ---
# SEED_COMPANY_NAME=Empresa Demo
# SEED_ADMIN_EMAIL=admin@erp.local
# SEED_ADMIN_PASSWORD=Admin@123
# SEED_ADMIN_NAME=Administrador
